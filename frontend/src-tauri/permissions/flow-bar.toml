[[permission]]
identifier = "flow-bar-commands"
description = "Allows the ambient Flow Bar to read the single native recording state."
commands.allow = [
  "flow_bar_start_recording",
  "get_recording_state",
]

# Native dragging lets users move the compact bar without exposing broader window controls.
